# ABOUTME: Test that sign in button is visible
# ABOUTME: Converted from login.test.js "should show sign in button"

appId: ai.dravr.app.dev

---
# Launch app fresh
- runFlow:
    file: ../helpers/launch-app.yaml

# Verify login button is visible
- assertVisible:
    id: "login-button"
