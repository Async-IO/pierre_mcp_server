# ABOUTME: Test that hardcoded user stats do NOT exist
# ABOUTME: Converted from settings.test.js "hallucinated elements - must NOT exist"
# ABOUTME: These were previously hardcoded values that don't come from backend

appId: com.asyncio.pierre

---
# Launch and login
- runFlow:
    file: ../helpers/launch-app.yaml
- runFlow:
    file: ../helpers/login.yaml

# Navigate to Settings
- runFlow:
    file: ../helpers/navigate-to-settings.yaml

# Test: should NOT show hardcoded user stats (activities, hours, insights)
- assertNotVisible:
    text: "127"
    timeout: 2000

- assertNotVisible:
    text: "89"
    timeout: 2000

- assertNotVisible:
    text: "12"
    timeout: 2000
