# ABOUTME: Helper flow to send a chat message
# ABOUTME: Types a message and sends it, optionally waits for response

appId: ai.dravr.app.dev

---
# Type message in the input field
- tapOn:
    id: "message-input"
- inputText: ${MESSAGE}

# Tap send button
- tapOn:
    id: "send-button"
