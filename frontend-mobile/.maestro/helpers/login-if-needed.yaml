# ABOUTME: Conditional login helper that only logs in if not already authenticated
# ABOUTME: Checks for login screen and performs login if visible, otherwise skips

appId: ai.dravr.app.dev

---
# Check if we're on login screen
- runFlow:
    when:
      visible:
        id: "login-screen"
    file: login.yaml
